name: movie_tracker
description: Film ve dizi takip uygulamasi - CineTrack
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter_dotenv: ^5.2.1
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  google_fonts: ^6.2.1
  http: ^1.2.1
  fl_chart: ^0.68.0
  cached_network_image: ^3.3.1
  intl: ^0.19.0
  supabase_flutter: ^2.16.0
  flutter_secure_storage: ^10.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  assets:
    - .env
